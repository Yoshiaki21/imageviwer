# imageviewer 実装指示書

Rust言語 + gpui + gpui-component を使用した、Windows11 / Manjaro KDE (Linux) 両対応の画像ビュワーアプリケーション「imageviewer」の実装指示書。

## 0. 開発方針・優先順位

1. **まずManjaro KDE (Linux) で動作するものを実装する**
2. その後、Windows11で動作するように改良・差分対応する
3. 「同じ挙動になっていればOSごとに異なる実装方式を採用してよい」箇所（単一インスタンス制御、既存ウィンドウへのファイルパス受け渡しなど）は、後述の通りOSごとに最適な方式で個別実装してよい。ただし**ユーザーから見た挙動は両OSで完全に同一**にすること。

## 1. 対象プラットフォーム

- Windows 11
- Manjaro KDE (Linux)

両OSで動作するコードベースとすること。プラットフォーム固有の実装が必要な箇所は `#[cfg(target_os = "windows")]` / `#[cfg(target_os = "linux")]` 等で分岐してよい。

## 2. アプリ名

`imageviewer`

## 3. 単一インスタンス制御・ファイルパスの受け渡し

### 要件（両OS共通の挙動）
- ビュワーは同時に1つのウィンドウしか存在しない（多重起動しない）
- 既にビュワーが起動している状態で、別の画像ファイルに対して起動要求（ファイル関連付け等）が発生した場合、新しいプロセスは起動せず、**既に起動しているウィンドウ**に画像パスを渡し、そのウィンドウの表示を更新する
- この際、**既存ウィンドウの位置・サイズは変更しない**（表示画像だけが切り替わる）

### 実装方式
- 実現方式はOSごとに最適な手段を選んでよい（例：Windowsは名前付きMutexでの多重起動検知＋名前付きパイプでのパス受け渡し、Linuxは名前付きソケットやDBus等）。挙動さえ一致していれば、OSごとに異なる技術的実装でよい。

## 4. 操作仕様

| 操作 | 内容 |
|---|---|
| → | 同じフォルダ内の「次」の画像を表示 |
| ← | 同じフォルダ内の「前」の画像を表示 |
| ↑ | 現在のフォルダと同じ階層にある「前」の兄弟フォルダへ移動し、そのフォルダの画像を表示 |
| ↓ | 現在のフォルダと同じ階層にある「次」の兄弟フォルダへ移動し、そのフォルダの画像を表示 |
| Enter | フルスクリーン ⇔ ウィンドウモードの切り替え |

### 補足
- 「同じフォルダ内の次/前」の順序は、**ファイル名の自然順ソート**（数値を含む場合も違和感のない順序。例: `img2.jpg` → `img10.jpg` の順になるようにする）とする。
- ↑↓で移動する「兄弟フォルダ」は、現在のフォルダと同じ親ディレクトリを持つフォルダ群を名前順に並べたものとする。
- **画像ファイル（現状 png/jpg）を1枚も含まないフォルダは「存在しないもの」として扱い**、↑↓移動時は完全にスキップして次の該当フォルダを探す。
- 移動先の兄弟フォルダ内の画像は、そのフォルダの先頭（ソート順で最初）の画像を表示する。

## 5. 画像表示仕様

- アスペクト比を維持したまま、ウィンドウ全体（フルスクリーン時も同様）に収まるように表示する（レターボックス表示）
- 背景色：黒
- 拡大・縮小（ズーム）機能：**現時点では実装不要**。将来追加できるよう、表示ロジックはズーム機能を後付けしやすい構造にしておくことが望ましい（必須ではない）

## 6. 対応画像フォーマット

- 現時点でサポートするのは **PNG, JPG のみ**
- 将来的に BMP, GIF 等を追加する可能性があるため、画像デコード処理は抽象化しておくこと

### 実装方式（拡張子↔デコーダのマッピング）
**enum + trait方式**を採用する。

```
// イメージ（実際の実装はClaude Codeの判断で調整してよい）
enum ImageFormat {
    Png,
    Jpg,
}

trait ImageDecoder {
    fn decode(path: &Path) -> Result<DecodedImage>;
}
```

- 拡張子文字列 → `ImageFormat` enum への変換を行う
- `ImageFormat` ごとに対応する `ImageDecoder` 実装を呼び出す
- 将来フォーマットを追加する際は `ImageFormat` にバリアントを追加し、対応する変換・デコーダ実装を追加するだけで済むようにする（Rustのmatch網羅性チェックを活用し、対応漏れをコンパイル時に検出できる設計とする）

## 7. ウィンドウの起動位置・モード

- **起動時は常にウィンドウモード**（フルスクリーン状態は次回起動時に引き継がない）
- **既に別プロセスが起動していて、それに画像パスを渡す場合**：既存ウィンドウの位置・サイズは変更せず、表示画像のみ更新する
- **新規にプロセスが起動する場合**：`config.toml` に保存されている前回終了時のウィンドウ位置・サイズを復元する
- ウィンドウの最小サイズ：仮に 400×300px 程度を下限とする（要調整可）

## 8. 起動時の画像復元ロジック（引数なし起動時）

コマンドライン引数でファイルパスが指定されずに起動された場合の挙動：

1. `config.toml` に「最後に開いたフォルダ」の設定が **存在しない** 場合
   → その旨をメッセージ表示し、アプリを終了する
2. 「最後に開いたフォルダ」の設定は存在するが、
   - フォルダ自体が存在しない、または
   - フォルダ内に画像ファイル（png/jpg）が1枚もない
   → 問題を通知し、アプリを終了する。**同時に `config.toml` から「最後に開いたフォルダ」「インデックス」の設定項目を削除する**
3. フォルダが存在し画像もある場合：
   - 保存されている**インデックス番目のファイルが存在する** → それを表示する
   - インデックス番目のファイルが**存在しない**（削除・移動等） → 現在のフォルダ内ファイルリスト（自然順ソート後）の中で、保存インデックスに**最も近いインデックス**のファイルを表示する
     - 前後で距離が同じ場合は、**小さい方（前方）を優先**する

### 「次へ/前へ」操作時のファイル欠損
- 「次へ」「前へ」で対象となるファイルが実際には存在しない（削除・移動されていた）場合、そのファイルは無いものとして扱い、さらに次（前）のファイルへ進む

### 画像デコード失敗時
- 画像ファイルが壊れている、またはデコードできない場合も同様に「無いものとして扱い」、次の画像へ進む挙動とする（起動時・次へ/前へ操作時ともに共通）

## 9. 設定ファイル（config.toml）

- **配置場所**：実行ファイルと同じディレクトリ
- **ファイル名**：`config.toml`
- **形式**：TOML（手動編集のしやすさを優先して選定）

### 保存項目
- ウィンドウの位置（x, y）
- ウィンドウのサイズ（width, height）
- 最後に開いたフォルダのパス
- 最後に開いたフォルダ内でのファイルリストのインデックス
- ログ出力設定（後述）

### config.toml イメージ

```toml
[window]
x = 100
y = 100
width = 800
height = 600

[last_opened]
folder = "C:\\Users\\yoshiaki\\Pictures\\sample"
index = 5

[logging]
enabled = true          # ログ出力する/しない（未指定時のデフォルト: true）

# 詳細ログ（デバッグ情報）を出力するかどうかの設定。
# デバッグビルドでは常にこの設定に関わらず詳細ログを出力する。
# リリースビルドでは、この項目が未設定または false の場合はエラーのみ出力する。
# 実運用中に不具合の手がかりが欲しい場合のみ、以下のコメントを外して true にしてください。
# verbose = false
```

## 10. ログ出力仕様

- **出力先**：実行ファイルと同じディレクトリに `viewer.log`
- **出力モード**：追記し続ける（ローテーションや上書きは行わない。肥大化した場合はユーザーが手動削除する運用とする）
- **ON/OFF**：`config.toml` の `[logging] enabled` で制御。未設定時のデフォルトは `true`（出力する）
- **詳細度**：
  - **デバッグビルド**（`cargo build`）：常に詳細ログ（デバッグ情報込み）を出力する
  - **リリースビルド**（`cargo build --release`）：デフォルトはエラーのみ出力。`config.toml` の `[logging] verbose = true` を設定した場合のみ詳細ログを出力する
  - ビルドモードの判定には `cfg!(debug_assertions)` を利用する
- **出力内容（エラーのみモード時）**：エラー発生箇所・内容を記録する
- `config.toml` には `verbose` 設定項目を**コメントアウトした状態**でサンプルと説明文を含めておくこと（上記イメージ参照）

## 11. ファイル関連付け

- アプリ本体のコードとしては、**起動時にコマンドライン引数として渡された画像ファイルパスを受け取り、既存インスタンスがあればそちらに渡す**ところまでを実装範囲とする
- OS側の「.pngや.jpgをimageviewerに関連付ける」設定（Windowsのレジストリ登録、KDEの`.desktop`ファイル/MIME関連付け）は、アプリのコード実装範囲外とし、別途セットアップ手順として文書化する（インストーラー等は今回のスコープ外）

## 12. スコープ外（今回実装しない機能）

- 拡大・縮小（ズーム）機能
- BMP, GIF等 PNG/JPG以外の画像フォーマット対応
- 多言語対応（i18n）
- ログのローテーション機能
- ログレベルの詳細な段階分け（エラーのみ／詳細の2段階のみ）
